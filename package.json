{
  "name": "coffre",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply coffre-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply coffre-db --remote"
  },
  "devDependencies": {
    "wrangler": "^4.120.0"
  }
}
